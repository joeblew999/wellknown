# Local development environment (contains secrets)
.env.local

# Generated env files (if you create them for testing)
.env
.env.production
.env.local.template
.env.production.template

# Secrets files (plaintext - NEVER commit!)
.env.secrets
.env.secrets.local
.env.secrets.production

# Encrypted secrets files are SAFE to commit (*.age files)
# These are tracked by git

# Fly.io secrets export
fly-secrets.txt

# Build artifacts
bin/
*.exe
*.dll
*.so
*.dylib

# Don't ignore the example secrets file (safe to commit)
# .env.secrets is committed for demonstration purposes
.bin/
example

