# If you prefer the allow list template instead of the deny list, see community template:
# https://github.com/github/gitignore/blob/main/community/Golang/Go.AllowList.gitignore
#
# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Code coverage profiles and other test artifacts
*.out
coverage.*
*.coverprofile
profile.cov

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work
go.work.sum

# env file
.env

# Editor/IDE
# .idea/
# .vscode/

# MCP Configuration (personalized per developer)
# Keep .vscode/mcp.json.example (template) but ignore personalized configs
.vscode/mcp.json

# mcp claude
.playwright-mcp

.bin/
.dist/
tmp/
/server

# Multi-service data directory
# All runtime data (databases, uploads, backups, cache, logs)
.data/
!.data/.gitkeep

# Optional: Track TypeScript definitions for development (uncomment if needed)
# !.data/pb/types.d.ts

# PocketBase runtime subdirectories (inside .data/pb/) - explicitly listed for clarity:
# - storage/           File uploads organized by collection
# - backups/           Automated database backups (*.zip)
# - .pb_temp_to_delete/ Temporary files (auto-cleaned by PocketBase)
# - .autocert_cache/   Let's Encrypt SSL certificates
# These are already ignored via .data/ pattern above

# Fly.io (keep fly.toml for deployment config)
.fly/
